# Residual Juggling Workspace Setup

```bash
git clone git@github.com:kploeger/vcs_residual_juggling_ws.git
cd vcs_residual_juggling_ws
sudo apt-get update && sudo apt-get install -y python3-vcstool
bash setup_workspace.sh
```
